asgiref==3.8.1
certifi==2025.1.31
cffi==1.17.1
channels==4.2.0
channels_redis==4.2.1
charset-normalizer==3.4.1
colorlog==6.9.0
cryptography==44.0.0
daphne==4.2.3
Django==5.1.5
djangorestframework==3.15.2
gunicorn==23.0.0
idna==3.10
psycopg2-binary==2.9.10
pycparser==2.22
# Pinned below channels_redis's tested range -- channels_redis==4.2.1 has no
# upper bound on its own `redis` dependency, so an unpinned install pulls
# redis>=8, which has real async timeout/connection-pool incompatibilities
# with it (reproduced locally: the channel layer's background listener
# crashes with a spurious redis.exceptions.TimeoutError under load).
redis==5.0.8
requests==2.32.3
sqlparse==0.5.3
urllib3==2.3.0